spring:
  application:
    name: gestionalePizzeria
  datasource:
    url: jdbc:mysql://localhost:3306/pizzeria_db?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
